import { LabelCheckedMapFilterPipe } from './label-checked-map-filter.pipe';

describe('LabelCheckedMapFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelCheckedMapFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
